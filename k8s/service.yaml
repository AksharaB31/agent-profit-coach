apiVersion: v1
kind: Service
metadata:
  name: agent-profit-api
  labels:
    app: agent-profit-api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: agent-profit-api
